/*
 * Copyright © 2024 Telenav, Inc. All rights reserved. Telenav® is a registered trademark
 * of Telenav, Inc.,Sunnyvale, California in the United States and may be registered in
 * other countries. Other names may be trademarks of their respective owners.
 */

package com.telenav.sdk.examples.util

// Keep in mind these style resources are placed in the corresponding path or included in Atlas resources
object StyleConstants {

    /** Path to the default style file */
    const val DEFAULT_STYLE_PATH = "newstyle.tss"

    /** Path to the cluster style file */
    const val CLUSTER_STYLE_PATH = "styles/cluster/newstyle.tss"

    /** Path to the intersection view style file */
    const val INTERSECTION_VIEW_STYLE_PATH = "styles/intersection_view/newstyle.tss"

    /** Path to the HUD style file */
    const val HUD_STYLE_PATH = "styles/hud/newstyle.tss"

    /** Path to the warm HUD style file */
    const val HUD_WARM_STYLE_PATH = "styles/hud_warm/newstyle.tss"

    /** Path to the invalid style file */
    const val INVALID_STYLE_PATH = "invalid/newstyle.tss"

    /** Absolute path to the local warm HUD style file */
    const val LOCAL_ABSOLUTE_WARM_STYLE_PATH = "/sdcard/download/style/hud_warm.tss"

    /** Full path to the default style file */
    const val DEFAULT_STYLE_FULL_PATH = "styles/default/newstyle.tss"
}
